<p><%= t("devise.links.hello") %> <%= @resource.email %>!</p>

<p><%= t("devise.label.unlock") %></p>

<p><%= t("devise.label.unlock_link") %></p>

<p><%= link_to t("devise.links.unlock_account"), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
