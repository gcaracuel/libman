<p><%= t("devise.links.hello") %> <%= @resource.email %>!</p>

<p><%= t("devise.label.confirm_link") %></p>

<p><%= link_to  t("devise.links.confirm_account"), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
